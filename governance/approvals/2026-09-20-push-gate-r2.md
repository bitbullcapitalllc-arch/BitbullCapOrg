---
record_type: push-approval
record_id: 2026-09-20-push-gate-r2
approved_commit: 18b3b6aef6858a8d98e2a6451de815f20beed4fe
remote: origin
branch: claude/bitbull-capital-org-structure-eiiv8c
qa_signed_by: qa-tester
qa_date: 2026-09-20
qa_verdict: PASS WITH NOTED RISK
qa_verified_from: fresh-clone
cto_signed_by: cto
cto_date: 2026-09-20
cto_decision: APPROVED
---

# Push Approval Record

> **No push to GitHub without this record.** QA fills section 2 and the `qa_*` lines. The CTO fills section 3 and the `cto_*` lines, **only after reading QA's evidence**. Nobody fills in another party's lines, and nobody fills in the `PENDING` values on behalf of someone else. The checklist is `governance/policies/push-checklist.md`; the pre-push hook (`scripts/check_push_approval.py`) reads the block above.
>
> Allowed values — `qa_verdict`: `PASS` · `PASS WITH NOTED RISK` · `FAIL` · `COULD NOT VERIFY`. `cto_decision`: `APPROVED` · `HELD`. Only `PASS` / `PASS WITH NOTED RISK` together with `APPROVED` lets a push through.
>
> **`approved_commit` is the commit QA verified.** This record is committed *after* it, in a commit that only **adds or modifies regular `*.md` files under `governance/approvals/`** — that, and nothing else, is what the hook allows in **every** commit between the approved commit and the pushed tip (no deletions, renames, symlinks, mode changes, submodule moves, non-`.md` files, merge commits, or `.` / `..` / `.git` path components).

## 1. What is being pushed

- **Commit(s):** three commits over the remote's `089caa1`, verified at the tip `18b3b6aef6858a8d98e2a6451de815f20beed4fe`:
  - `8079c73` — Add the push gate; fix HANDOFF and CLAUDE.md; document the recovery step
  - `8a99ecb` — Harden the push gate after QA's first verification failed; add resume section (**CTO HELD** this one; it is not approved on its own)
  - `18b3b6a` — Close the push gate's second-round holes; CEO authorization for CTO section 3
- **Branch → remote:** `claude/bitbull-capital-org-structure-eiiv8c` → `origin`
- **Nature of the change:** governance + tooling + tests + docs (the push gate itself, its checklist, its record template, and the documents describing them; no bot or `risk/` / `execution/` code)
- **Author of the change:** CEO session (governance text, docs, and the first two gate commits, `8079c73` and `8a99ecb`); `cto` (the gate fix in `18b3b6a`: `scripts/check_push_approval.py`, `.githooks/pre-push`, `tests/test_push_gate.py`). All three commits carry the same git author, so authorship per file is from the commit messages and `workspaces/engineering/work/2026-09-20-cto-push-gate-fix.md`, not from git. The CTO is both author of the gate code and the section 3 signer; the CEO's one-time, scoped authorization for that is the `governance/decision-log.md` entry "CEO authorizes the CTO to sign section 3 for the gate-code fix it authored (one-time, scoped)". QA did not author any of it.
- **Anything that could place a live order, touch a venue or hold a credential?** no — nothing in the range imports a venue client, reads a credential, or changes mode handling; the secret scan of the range's added lines found one hit, a fake string inside a test fixture (`tests/test_push_gate.py`, "not-a-real-secret").

## 2. QA verification — `qa-tester`

- **Verified from a fresh clone of commit:** `18b3b6aef6858a8d98e2a6451de815f20beed4fe` (output of `git rev-parse HEAD` in `git clone --no-hardlinks` of the repo; `git status --short` empty before and after the runs)
- **Checklist section B, item by item:**
  - Clean and complete clone — pass
  - Bot suite — pass against the known set (55 / 6)
  - Org tooling suite — pass against the known set (43 / 8)
  - Push-gate suite — pass (63 / 0)
  - No new red — pass
  - `spec_lint` — pass; `check_boundaries --audit` — pass
  - Boundary check for the authors' roles — **violations, explained**: see the evidence file
  - Fixture render — pass (3 of 3)
  - Doc links and mermaid fences — pass (fence balance only; no Mermaid parser exists here)
  - Documentation claims (161 passed / 14 failed total) — reproduced
  - Risk-critical paths — `risk/` and `execution/` untouched; the approval machinery is what changed and was attacked (below)
  - Real commands and output, and the whole attack log: `governance/approvals/2026-09-20-qa-evidence-gate-r2.md`
- **Bot suite:** 55 passed, 6 failed — failing tests: `test_import_graph.py::TestPackageSkeleton::test_every_required_subpackage_exists`, `…::test_tests_mirror_src_for_data_package`, `test_no_float_money.py::TestNoFloatMoney::test_bar_dataclass_declares_decimal_for_every_money_field`, `…::test_loader_parses_money_cells_via_decimal_not_float`, `…::test_money_path_files_exist`, `…::test_no_float_dtype_or_builtin_used_on_a_money_value` (all six the missing data loader; exact names as printed are in the evidence file)
- **Org tooling suite:** 43 passed, 8 failed (`tests/test_tooling.py`) — failing tests: `TestS3FrontmatterParsing` x4, `TestGitignoredPaths::test_include_ignored_*` x2, `TestExistingBehaviourPreserved` x2 — exactly the known set. `tests/test_push_gate.py`: **63 passed, 0 failed**.
- **Failures outside the known set (blockers):** none
- **spec_lint / boundary audit / fixture render / doc link check:** PASS / PASS / 3 of 3 rendered / 22 files, 65 links, 29 mermaid blocks, 0 problems
- **Documentation claims that QA could not reproduce:** none
- **Residual risk being accepted:**
  1. The gate is fail-open when `.githooks/pre-push` is absent from the checked-out tree (for example a branch built on `089caa1`, which predates the hook): git silently skips a missing hook. Also `--no-verify`, a clone without the hook, grafts written by a local actor, and GitHub API pushes — the last needs GitHub branch protection, a founder-side setting still not enabled.
  2. Commits between the remote tip and the approved commit are not checked by the gate; QA scanned the added lines of `089caa1..18b3b6a` for secrets by hand (clean), but no checklist step requires this.
  3. Record authorship is not verified; any `*.md` under `governance/approvals/` may ride along.
- **Re-verification (2026-09-20, second fresh clone of `18b3b6a`, `git rev-parse HEAD` confirmed):** section 2 was **reproduced exactly** — bot 55/6, org tooling 43/8, push gate 63/0, 161/14 total, `spec_lint`/audit PASS, fixtures 3 of 3, doc links 22/65/29/0, the five closed bypasses all still BLOCK end to end. The boundary violations are now explained in section 7 of `governance/approvals/2026-09-20-qa-evidence-gate-r2.md` (CEO-authored first gate code in `8079c73`/`8a99ecb`, disclosed in the decision log; plus 2 ungoverned paths, `.gitattributes` and `HANDOFF.md`). New, non-blocking notes from the re-attack (evidence section 13): the checker that runs is the working tree's, not the pushed tip's (a stale checkout weakens the gate); Unicode-ignorable `.git` look-alike path components are not rejected (`.md`-only, theoretical); malformed or empty hook stdin exits 0. Verdict unchanged.
- **Verdict:** PASS WITH NOTED RISK

## 3. CTO review

- **QA evidence read:** **yes.** `governance/approvals/2026-09-20-qa-evidence-gate-r2.md`
  (1,678 lines) now exists and is the artifact my hold asked for: the `git clone --no-hardlinks`
  and `git rev-parse HEAD` transcript, verbatim output of all three suites, `spec_lint`, the
  boundary audit, the per-role boundary check **with the violations explained in prose** (§7),
  the doc-link/mermaid and fixture checks, the per-commit secret scan, and the full attack log
  — written incrementally, with its own harness errors disclosed in place. I read §7, §11a–g
  and §13, not the verdict line. Section 2 reproduces exactly (bot 55/6, org tooling 43/8,
  push gate 63/0, total 161/14; spec_lint PASS; audit PASS over 32 messages; fixtures 3 of 3;
  doc links 22/65/29/0), and those numbers now have two independent origins: QA's fresh clone
  and my own earlier measurement. That is precisely what the hold was for, and it is cleared.
- **Diff reviewed — parts read in full:** `scripts/check_push_approval.py` (357 lines at
  `18b3b6a`), `.githooks/pre-push` (17 lines), the `workspaces/registry.json` diff (one
  addition, `".githooks/**": ["cto"]`, made by the CEO session in `8079c73`), and the
  per-commit file list of `18b3b6a`. Range `089caa1..18b3b6a`: 25 files, +1552 / −23.
  Findings: the hook fails closed with no interpreter and resolves its own repo root with
  `--no-replace-objects`; every checker git call carries `--no-replace-objects`,
  `diff.ignoreSubmodules=none` and `core.quotePath=false`; `changes_after()` walks the range
  commit by commit, refuses any commit without exactly one parent, and refuses if the
  end-to-end cross-check disagrees with the walk; `tree_changes()` admits only `A`/`M`, mode
  `100644`, `governance/approvals/` prefix, `.md` suffix, safe path shape; `unsafe_path()`
  covers `.`, `..`, backslashes and `.git` variants including NTFS trailing-dot folding and
  `git~1`; `parse_record()` refuses duplicate and quoted keys; `evaluate()` refuses ref
  deletion, non-branch refs, an unfetched remote tip and any non-fast-forward; no override
  flag exists. QA's attack log now exercises all of that end to end through the real hook.
- **Section A of the checklist honestly completed:** yes. The §B boundary violations are now
  **explained** (below), which was the one item I recorded as unexplained at my hold. Nothing
  in the range imports a venue client, reads a credential, places an order or touches mode
  handling; `risk/` and `execution/` are untouched.
- **Force-push or ref deletion:** no — fast-forward of `089caa1` on
  `claude/bitbull-capital-org-structure-eiiv8c`. Remote URL read by eye per §D:
  `git remote -v` → `https://github.com/bitbullcapitalllc-arch/BitbullCapOrg.git` (fetch and
  push), the firm's repository and nothing else.
- **Higher gate needed and missing:** none. Governance, tooling, tests and docs only.
- **Decision:** **APPROVED**
- **Conditions / reasons:** The sole ground for the hold — a cited QA evidence file that did
  not exist — is cleared by an artifact I have read. **The four items new since my hold are
  each accepted as documented residual risk; none blocks this push.**
  **(1) Stale-checkout leak (evidence L5) — ACCEPT.** The hook and checker that execute are
  the *working tree's*, not the pushed tip's: with `8a99ecb` checked out an add-then-remove
  leak reached QA's scratch remote (exit 0), while with `18b3b6a` checked out the identical
  push was refused (exit 1). Real, and correctly reported with both halves of the control. It
  does not block because it is inherent to the mechanism and the obvious alternative is worse
  — running the *tip's* checker would hand the gate to whoever authored the tip — and because
  it **degrades rather than opens**: a stale tree runs an older but genuine gate that still
  demands QA PASS + CTO APPROVED; what is lost is the newest closed bypasses, not the approval
  requirement. **Condition on this push:** it must be pushed from a working tree at the tip
  being pushed. Evidence §12 records this tree at `18b3b6a` with `core.hooksPath=.githooks`,
  and the record commit sits directly on it, so the checker that runs is `18b3b6a`'s —
  exposure nil. **Required in the next change that touches the gate** (deliberately not here,
  since a new commit means a new unverified commit and another loop): make the checker compare
  the running `scripts/check_push_approval.py` and `.githooks/pre-push` against the tip's
  copies and **BLOCK on mismatch**, with a tested carve-out for a tip lacking the files; add a
  §A line "push from a tree at the tip"; add the limit to the docstring.
  **(2) Unicode-ignorable `.git` look-alike components — ACCEPT.** `unsafe_path()`'s folding
  does not strip format characters, so `.g<U+200C>it/` and `.git<U+200C>/` are allowed. QA
  labels the host behaviour **unverified** and I will not improve on that from memory. Bounded:
  only `.md` files ride the exemption, and placing one already requires commit authorship under
  `governance/approvals/`. Fix with (1): reject a component whose format-stripped, case-folded
  form matches a forbidden name, with a regression test, and record the limit *as unverified*.
  **(3) Malformed or empty hook stdin exits 0 — ACCEPT.** Empty stdin exiting 0 is correct
  (nothing to push). Malformed stdin exiting 0 is the wrong default in principle, but git is
  the only producer of that stream and anyone who can feed the hook arbitrary stdin already has
  local execution and would use `--no-verify`, the documented R1 limit. Fix with (1): a line
  that does not split into exactly four fields is an error; empty stdin stays 0.
  **(4) The line-45 boundary violations — ACCEPTED AS EXPLAINED.** Evidence §7 establishes with
  per-commit `git show --name-only` that the CEO session authored the first versions of the
  three CTO-owned gate files in `8079c73`/`8a99ecb`; the five `--role ceo` violations are
  exactly those files, the `--role cto` run over my own five files is **clean**, and the 18
  violations in the `--role cto`-over-everything run are an artefact of feeding one role the
  whole range. The breach is real, disclosed in `governance/decision-log.md` as a
  founder-directed exception, and already remedied in the way that matters: I rewrote that code
  in `18b3b6a` and own it from here. QA's statement that it can confirm the log's *text* but
  not that the founder *gave* the direction is the correct limit of what a repository can
  prove; that is a provenance item for the CEO to put in front of the founder, not a technical
  blocker at the QA→CTO gate. **New minor gap:** `.gitattributes` and `HANDOFF.md` are covered
  by no registry write rule, so that check exits 1 partly for a non-violation reason;
  `workspaces/registry.json` is CEO-owned, so this is a CEO follow-up — and it matters twice,
  because anything that later consumes that exit code automatically will read as failing.
  **Harness mistakes — none touches a gate result.** The test for a discarded run is whether
  the re-run's control can still produce an ALLOW; every re-run here has one. (a) The B4
  `update-ref` failed, so the remote never diverged and the following push was an ordinary
  fast-forward, correctly allowed — not a gate result, and settled by B4b/B4c/B4d, where
  non-fast-forward, explicit `--force` and an unfetched remote are all refused with the right
  reasons. (b) The first attack-5 run passed a zero-padded short `--remote-sha`, so everything
  including the control blocked with "fetch first"; the re-run with the full SHA has a control
  that ALLOWs and 20 probes matching expectation. (c/d) The two `craft()` bugs trace to
  `grep -P` being unsupported in this Git Bash locale, which emptied the filtered tree listings
  so every crafted tip deleted the repository; discarded and re-run with `awk`, then a final
  harness whose control ALLOWs and whose 19 path probes each match. Four errors disclosed in
  place with re-runs raises my confidence in this evidence rather than lowering it.
  **Operational note for the commit that carries this record** (checked in the code, not
  assumed): `evaluate()` returns on the **first valid** record and treats invalid candidates as
  findings, not a veto, so the superseded HELD `push-gate-hardening.md` will not block; the two
  `qa-evidence-*.md` files are not parsed as records at all. **Commit only `*.md` files under
  `governance/approvals/`** — the working tree's frontend UI files, the CFO note and my own
  engineering work note are outside the exemption and the gate will refuse the push if they are
  added. That is the gate working. QA's evidence file contains one credential-shaped string,
  `AKIAIOSFODNN7EXAMPLE` with its matching example secret, used deliberately as an attack
  payload in L7 to show the gate does not scan approvals content; it is AWS's published
  documentation example, **not a secret**, and I name it so the next scan's hit is a known
  false positive.
  **Still owed to the founder, and not closed by this approval:** GitHub **branch protection**
  is the only complete control, is founder-side, and is still not enabled. Every limit
  reproduced in evidence §11g — `--no-verify`, `-c core.hooksPath=/dev/null`, a fresh clone
  with no hook installed, a tree with no `.githooks/`, an edited uncommitted checker, a record
  flipped by a later approvals-only commit — is in that class, and approving this push closes
  none of them. Also still open: the template's "the author must not be the signer of sections
  2 or 3" conflicts with `scripts/**` being CTO-only; my signature here rests on the CEO's
  one-time scoped authorization in `governance/decision-log.md`, and I want that dependency
  visible rather than tidied away. Required follow-up carried forward from my hold: add a §B
  item — "scan the added lines of `<remote tip>..<tip>` for secrets, commit by commit" — in the
  next change that touches the checklist.
  Full reasoning: `workspaces/engineering/work/2026-09-20-cto-push-gate-r2-decision.md` (not in
  this push — it is outside the approvals exemption — so this section is written to stand alone).

## 4. Signatures

| Role | Agent | Date | Verdict |
|---|---|---|---|
| QA | `qa-tester` | 2026-09-20 | PASS WITH NOTED RISK |
| CTO | `cto` | 2026-09-20 | APPROVED — evidence read and re-verified; four new residual risks accepted; push from a tree at the tip |

**No approval with an open blocker, a failure outside the known set, or a check that was not executed.**

## 5. After the push

- **Pushed by:** `<role>` · **Remote tip after push (`git ls-remote`):** `<sha>` · **Equals approved tip:** yes / no
- **Issues observed:**
