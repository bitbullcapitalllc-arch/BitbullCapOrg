# Secrets — never commit credentials, API keys or account numbers (see CLAUDE.md)
.env
.env.*
!.env.example
*.pem
*.key
credentials*
secrets*
config.local.*

# Trading/runtime output that must not live in the repo
logs/
*.log
data/
*.csv
*.parquet

# Python / Node / editor noise
__pycache__/
*.py[cod]
.venv/
venv/
node_modules/
dist/
build/
.DS_Store
.ipynb_checkpoints/
