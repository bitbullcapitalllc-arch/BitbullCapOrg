"""bitbull.obs — AlertSink, heartbeat writer (engine contract §1, §13).

PLACEHOLDER PACKAGE this round. Build order B1.11. `FileAlertSink` (append-
only JSONL under `runs/`, non-zero exit code, fail-closed if unopenable) and
the heartbeat/watchdog pair are not implemented here yet.
"""
